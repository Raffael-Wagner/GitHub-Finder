/**
 * Componente de erro usado quando na busca um usuário não é encontrado.
 *
 * Quando chamado, exibe uma mensagem simples de feedback.
 */
const Error = () => {
  return (
    <div>
      <p>Usuário não encontrado!</p>
    </div>
  );
};
export default Error;
